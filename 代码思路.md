# 知言 · 功能与代码思路

配合压缩包一起看。括号里的是代码中的函数名，在 index.html 里搜这个名字就能找到对应的代码。

---

## 0. 整体结构

没有用框架，也不需要编译，一共三个文件：

| 文件 | 干什么 |
|---|---|
| `index.html` | 整个 App：界面（HTML + CSS）和全部逻辑（最下面的 `<script>`） |
| `sw.js` | Service Worker：离线缓存、接收推送、弹通知 |
| `worker.js` | 放在 Cloudflare 上的小服务器：转发接口、定时让 TA 主动发消息 |

数据全部存在手机浏览器的 **IndexedDB** 里（`DB.get / DB.set`），分两个库：
- `kv`：设置、聊天记录、记忆、日历、心愿、歌单、图片（图片存成 Blob）
- `secrets`：API Key。用 WebCrypto 的 AES-GCM 加密后再存，加密密钥是 `non-extractable` 的，JS 读不出来（`saveSecret / loadSecret`）

### 页面布局
- 底部五栏：对话（`vChat`）、动态（`vFeed`）、记忆（`vMem`）、生活（`vLife`）、我的（`vMe`），用 `showTab` 切换，每次切过去时重新渲染（`renderMemories / renderLife / renderMe`）
- 左上角按钮打开聊天记录抽屉（`.side`），右上角 ＋ 开新对话
- 日历、一起听、心愿单、心情日记、TA 的设定、设置都是弹出的面板（`openSheet`）
- 生活页的"生活服务"是一组外部链接（`SERVICES`），想加别的 App 在这个数组里加一行
- 打字时自动收起底部栏（`body.typing`）；发送按钮上阻止了 `pointerdown` 的默认行为，点发送时输入框不会失焦，否则底部栏会弹回来，把按钮挤走

---

## 1. 聊天和流式输出（`streamChat`）

- 各家接口分成两类：
  - **OpenAI 兼容**：DeepSeek、OpenAI、智谱，都是 `POST /chat/completions`，`stream: true`
  - **Anthropic**：`POST /v1/messages`。网页直接调用要加请求头 `anthropic-dangerous-direct-browser-access: true`
- 返回的是 SSE：一行一个 `data: {...}`。用 `res.body.getReader()` 一边收一边拆行（`lines`），拼到消息里
- 画面刷新用 `requestAnimationFrame` 节流，不会每来一个字就重画一次
- DeepSeek 推理模型会返回 `reasoning_content`，单独放进"TA 在想…"的折叠框里
- **连不上（CORS）怎么办**：有的供应商不让网页直接调用，就在设置的"接口地址"里填 `worker.js` 的转发地址。Worker 帮你转一手，再加上允许跨域的响应头

---

## 2. 人设和系统提示（`systemPrompt`）

每次发消息时临时拼一段系统提示，内容依次是：
1. 人设：名字、关系、性格、TA 怎么叫你
2. 相处原则：关心但不控制，不用内疚感留住对方
3. **现在的日期时间** + 在一起第几天
4. 标签规则（见第 3 节）
5. 记忆列表
6. 未来 14 天的日程
7. 最近 3 条心情日记
8. 正在播放的歌

聊天记录只带最近 40 条，总字数超过 6 万就从最早的开始丢（`buildContext`）。

---

## 3. 核心技巧：标签 → 卡片

**这是整个陪伴功能的骨架。** 不用 function calling，而是让模型在回复末尾写约定好的标签，App 负责解析：

```
[[日程|2026-09-26|19:00|一起去看海]]
[[歌|晴天|周杰伦]]
[[心愿|拍立得]]
[[小红书|厦门看海攻略]]
[[记住|喜欢看海]]
[[画|黄昏的海边，两个人坐在沙滩上，温柔插画风]]
```

- 正则 `TAG_RE` 把标签匹配出来
- **流式输出过程中**用 `stripTags` 把标签藏起来，连写了一半的 `[[日...` 也去掉，所以用户看不到这些标记
- 回复结束后 `applyTags` 做两件事：存数据（进日历、进心愿单……），再生成卡片（`cardHTML`）
- 优点：所有模型都能用，包括不支持工具调用的，加新功能也只要多一种标签

---

## 4. 日历和纪念日

- 每个事件的格式是 `{ date, time, title, anniv }`。`anniv` 为 true 表示每年都算，只比较月和日（`eventsOn`）
- 标题里带"纪念日 / 生日 / 周年"的自动标成每年重复
- `upcoming(n)` 取未来 n 天的安排，用在两个地方：首页的"接下来"，以及塞进系统提示，让 TA 知道你的安排

---

## 5. 一起听

- 用户选的本地歌曲直接以 Blob 存进 IndexedDB（`track:<id>`），播放时 `URL.createObjectURL` → `<audio>`
- 正在播放的歌名会写进系统提示，TA 就能和你聊这首歌（TA 只知道歌名，听不到声音）
- TA 推荐的歌生成网易云 / QQ 音乐的**搜索链接**（`LINKS`），点了跳转到对应 App。没有接任何音乐平台的接口

## 6. 心愿单 / 小红书

同样是拼搜索链接（`LINKS`）：把关键词拼进淘宝、京东、小红书的搜索网址。简单，但不会出错。

## 7. 记忆和日记

- 记忆就是一个字符串列表，每次都带进系统提示
- 用户说"请记住……"时前端直接存一条；模型输出 `[[记住|…]]` 也会存。提示词里让 TA 自己判断什么值得记，不用等用户开口——这条搭在正常回复里，不额外发请求
- 三处写入统一走 `addMemory`：去重时忽略标点、空格、大小写；总量封顶 `MEM_MAX`（80 条），超出丢最旧的。`MEM_USE` 与 `MEM_MAX` 保持一致，否则记忆页列出来的和 TA 真正知道的会对不上
- 导入 Claude / ChatGPT 官方导出的 `conversations.json`：两家格式不一样，分开解析（`importFile`）

---

## 8. 画图（`drawImage` / `drawCard`）

- 模型输出 `[[画|画面描述]]` → 生成一张状态为 `new` 的图片卡片 → 调画图接口
- 智谱：`POST /images/generations`，model 用 `cogview-3-flash`（免费），返回一个**临时 URL**。尽量把图片下载成 Blob 存起来，因为 URL 过一段时间会失效
- OpenAI：`gpt-image-1`，返回 `b64_json`，转成 Blob
- 画的过程中显示一个闪光占位块（CSS 动画 `shimmer`）。失败了显示原因和"再画一次"按钮
- 画过的图会以"（你给对方画了一张图：…）"的形式写回聊天记录，TA 记得自己画过什么

---

## 8.5 手机与生活（生活页）

- **权限卡片**（`renderPerms`）：点了才申请权限
  - 定位：`navigator.geolocation` 拿坐标（只保留两位小数），用 open-meteo 查天气、bigdatacloud 查城市名，两个接口都不需要 Key（`refreshWeather`）
  - 麦克风：浏览器自带的 `SpeechRecognition` 语音转文字，开启后输入框旁出现麦克风按钮（`toggleMic`）
  - 通知：申请通知权限，然后引导去配置"TA 主动找你"
  - 相册：跳到换头像或背景
- **账单**（`ledger`）：`{ date, type: out/in, amount, cat, src, note }`，按月统计、按类别画条形图
- **经期**（`period`）：记录每次开始和结束；记满两次以上，用最近 6 个周期算平均值，推算下次时间（`periodInfo`）
- **健康**（`health`）：按日期记喝水杯数和睡眠时长，显示最近 7 天
- **让 TA 知道**：`lifeContext` 把天气、喝水、睡眠、经期（要手动打开）、本月支出拼成一段，放进系统提示
- 新标签：`[[记账|支出或收入|金额|类别|备注]]`、`[[喝水|杯数]]`
- **小游戏**：2048，四个方向的移动都转换成"向左合并一行"（`slideRow`）来处理

## 8.6 让 TA 更像"活的"

- **发照片**：选的图先压缩到 1280px 的 JPEG，以 dataURL 存进消息（`imgs`）。发给模型时由 `wire` 转成各家的格式：OpenAI / 智谱用 `image_url`，Claude 用 `image` + base64；DeepSeek 看不了图，就换成一句说明。只有最近两条带图的消息真正发图，更早的换成文字，省额度。智谱会自动换成 `glm-4v-flash` 看图
- **TA 的动态**（`moments`）：`newPost` 用人设、时间、天气、最近的聊天、最近几条动态（避免重复）让模型写一条；开了画图的话，有三分之一的概率配一张图。回到 App 时，如果隔了 8 小时以上、白天、今天少于 3 条，TA 会自己发（`maybeAutoPost`）。评论后 TA 会回复（`commentPost`）。有新动态时"动态"栏会出现红点
- **我也发一条**（`publishMine`）：动态不再是 TA 单向的，用户也能发，可以配一张图（走 `shrinkPhoto` 压成 1280px JPEG，blob 存 IndexedDB 的 `img:<帖子 id>`）。帖子用 `who` 区分作者，没有这个字段的就是 TA 发的（老数据兼容）。发完 `taReplyToMine` 让 TA 来评论一条，失败也不打扰用户。注意 `taPosts()`：TA 的每日配额和"有新动态"的红点都只数 TA 自己的帖，否则用户一发帖就会把 TA 的自动动态挤掉
- **按键通话**（`openCall` / `callStart` / `callStop`）：对讲机式，不是实时通话。按住 → `SpeechRecognition` 转文字 → 松开走正常的 `send()` → 回复由 `send()` 结尾那个朗读钩子念出来（判断条件是 `settings.speak || inCall`，只此一处，不会重复念）。做不了随时插话，那需要流式语音接口，几家供应商里没有免费的。说的内容照常留在聊天记录里
- **回忆**：`firsts` 自动记下各种"第一次"（`markFirst`）；`album` 收集照片、TA 画的图、动态配图；`nextMilestone` 倒数下一个纪念日（在一起 100 天、200 天、周年，以及日历里的纪念日）
- **小金库**（`wallet`）：记的是用户真的存给 TA 的钱（钱放在用户自己的支付宝或微信小荷包里，知言只记账）。你转账会作为一条特殊消息发给 TA；TA 用 `[[红包|金额|一句话]]`、`[[礼物|礼物名|价格]]` 从里面拿钱给你，不能超过余额；红包要点"開"才领取，领了就是提醒你真的去花这笔钱
- **一起攒钱**：心愿有 `price`（目标价）和 `saved`（已攒）。聊天里说"为拍立得存了 100"，TA 会用 `[[攒钱|心愿名|金额]]` 记上；攒够时发一条庆祝消息，TA 会回应（`celebrate`）

## 9. 毛玻璃风格

- 在 `<html>` 上加一个 `glass` class，把颜色变量换成半透明，比如 `--card: rgba(255,255,255,.55)`
- 背景是单独一层 `.wall`，默认用 4 个 `radial-gradient` 叠成柔和的色块，跟着主题色变。换成照片时，给这一层加 `filter: blur()` 调模糊度
- 顶栏、输入框、气泡、弹窗用 `backdrop-filter: blur(18px) saturate(170%)` 做出磨砂感
- 高光边：`box-shadow: inset 0 1px 0 rgba(255,255,255,.75)`
- **性能提醒**：`backdrop-filter` 比较吃性能。背景照片的模糊是在背景那一层上只做一次（`filter`），比较省。如果低端安卓机上滑动聊天会卡，先把气泡（`.msg.user`、`.msg.bot .body`）上的 `backdrop-filter` 去掉，只留顶栏、输入框和弹窗的，看起来差别不大

---

## 10. TA 主动找你

### 10.1 打开 App 时 TA 先开口（`maybeOpener`）
- 切到后台时记下 `lastActive`；回来时如果已经过了 3 小时以上，就生成一条 TA 主动说的话
- 技巧：接口要求最后一条必须是用户消息，所以补一条占位"（对方刚打开 App，还没说话）"，再在系统提示里说明"对方隔了 X 小时才来，你先开口，不要抱怨"

### 10.2 App 关着也能收到消息（`worker.js` + `sw.js`）

```
手机 App ──同步（人设、记忆、最近聊天、日程、时间段）──▶ Cloudflare Worker（存进 KV）
Worker 每 10 分钟跑一次（Cron）：
  ├ 到了随机挑的时间 → 调模型写一句话 → 放进信箱 inbox → 发 Web Push
  └ 日程快到了       → 写一句提醒 → 同上
手机收到推送 → sw.js 去 Worker 的 /inbox 取内容 → 弹通知
打开 App → 把信箱里的消息放进聊天 → 回执（/ack）清空信箱
```

- **Web Push 用的是"空推送"**：只要用 VAPID（ES256 签名的 JWT）证明是自己发的，不用做复杂的内容加密。通知内容由手机收到后自己去取。VAPID 密钥是 Worker 第一次运行时自己生成的，存在 KV 里
- 每天的发送时间是在用户设的时间段里**分段随机**挑的，不会挤在一起；刚聊完 40 分钟内的那次会跳过
- 推送失效（返回 404 / 410）的设备会自动删掉
- 限制：安卓网页推送走谷歌的推送服务（FCM），国内要开梯子，手机上还要有谷歌服务

---

## 11. PWA

- `manifest.webmanifest`：名字、图标、`display: standalone`
- `sw.js` 的缓存策略：打开页面时先走网络，没网就用缓存；图片、脚本等资源先用缓存
- **每次更新都要把 `sw.js` 里的 `CACHE` 版本号加 1**，否则手机上一直是旧版本

---

## 12. 打包成安卓 APK 的两条路

1. **PWABuilder（pwabuilder.com）**：输入网址，直接生成 APK（TWA，底层还是 Chrome）
   - 优点：几分钟就好，代码不用改，Web Push 照样能用
   - 缺点：必须先把网页部署到 https 地址上
2. **Capacitor**：把这三个文件放进 `www/`，用 Android Studio 编译
   - 用 `CapacitorHttp` 发请求就**没有跨域问题**，不需要转发
   - 日程提醒可以换成 `@capacitor/local-notifications`：手机本地定时提醒，不需要服务器，也不依赖谷歌服务
   - 真正的推送还得接 FCM（`@capacitor/push-notifications`）
   - 缺点：要装 Android Studio，自己编译

---

## 13. 让自己的 AI 帮忙改的时候

- **一次只改一块**，并且说出函数名或 CSS 选择器。比如："只改 index.html 里 `html.glass` 那段 CSS，把气泡透明度调低"
- 不要让 AI 重写整个文件，最费额度，也最容易改坏别的功能
- 每改好一个功能就存一份，改坏了能退回去
- 想加新的陪伴功能，优先用"加一种新标签"的方式（第 3 节）：在 `TAG_RE` 加一个名字，在 `applyTags` 里处理，在 `cardHTML` 里画卡片，再在 `systemPrompt` 里写一句规则
